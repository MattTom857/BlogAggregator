This repository is my work for the "Build a Blog Aggregator in Go" course
 on boot.dev.

In order to run this code in an Ubuntu window,
 you'll need to install Postgres and Go.
sudo apt update
sudo apt install postgresql postgresql-contrib

You'll also have to install the gator command line interface.

You'll have to use goose to get the database system set up.